spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///orderdb
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

outbox:
  publish-interval-ms: 500
